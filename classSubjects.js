let subject_title = prompt("What is the subject title:");
let class_schedule = prompt("Enter the class schedule:");
let classroom = prompt("Enter your classroom:");
let class_instructor = prompt("Enter your instructor:");
let student_name = prompt("Enter you name:");

console.log("${student_name}is currently enrolled in ${subject_title}with a class schedule of ${class_schedule}at room ${classroom}. The instructor for the subject is ${class_instructor}.");
